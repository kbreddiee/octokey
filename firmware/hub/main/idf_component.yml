## espkvm hub dependencies.
## usb_host_hid is Espressif's official HID host class driver for the
## ESP-IDF usb_host stack (the driver specified for the S3) — the only
## dependency beyond ESP-IDF built-ins.
dependencies:
  idf: ">=5.2"
  espressif/usb_host_hid: "~1.0.1"
